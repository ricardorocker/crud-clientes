import { SobrenomeValidatorPipe } from './sobrenome-validator.pipe';

describe('SobrenomeValidatorPipe', () => {
  it('create an instance', () => {
    const pipe = new SobrenomeValidatorPipe();
    expect(pipe).toBeTruthy();
  });
});
